name: Temporary staging office read-only diagnostic
on:
  push:
    branches:
      - tmp/diag-thamer-match-2026-09-14
  workflow_dispatch:
permissions:
  contents: read
jobs:
  diagnose:
    runs-on: ubuntu-latest
    environment: staging
    timeout-minutes: 8
    env:
      FIREBASE_SERVICE_ACCOUNT_JSON: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Find today's staging office
        run: node scripts/tmp-find-today-office.mjs | tee "$RUNNER_TEMP/today-office-report.json"
      - name: Upload diagnostic evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: today-office-readonly-${{ github.run_id }}
          path: ${{ runner.temp }}/today-office-report.json
          if-no-files-found: warn
          retention-days: 2
