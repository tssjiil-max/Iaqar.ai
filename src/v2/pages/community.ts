import { renderPlaceholderPage } from "../components/placeholder.js";

export function renderCommunityPage(): HTMLElement {
  return renderPlaceholderPage({
    title: "مجتمع الوسطاء",
    body: "هذه المساحة محفوظة للمرحلة 6."
  });
}
