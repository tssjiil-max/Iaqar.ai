import { renderPlaceholderPage } from "../components/placeholder.js";

export function renderAgreementsPage(): HTMLElement {
  return renderPlaceholderPage({
    title: "الاتفاقيات",
    body: "هذه المساحة محفوظة للمرحلة 8."
  });
}
