{
  "name": "iaqar-ai",
  "version": "1.0.0",
  "private": true,
  "description": "IAQAR.AI — نظام تشغيل ذكي للمكاتب العقارية (استضافة ثابتة + Cloudflare Worker)",
  "scripts": {
    "build": "node scripts/check-syntax.mjs",
    "test": "node --test \"tests/**/*.test.mjs\"",
    "test:worker": "npm --prefix worker test",
    "test:all": "npm run build && npm test && npm run test:worker"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
